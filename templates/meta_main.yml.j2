---
# Generated file. Do not edit directly.
# Source template: {{ template_name }}

galaxy_info:
    namespace: guidugli
    role_name: fail2ban
    author: guidugli
    description: Install and configure Fail2Ban on Linux
    company: Creuzo Tech
    license: MIT
    min_ansible_version: '2.16'
    platforms:
{% for platform in platforms %}
        - name: {{ platform.name }}
          versions:
{% for version in platform.versions %}
              - {{ version }}
{% endfor %}
{% endfor %}
    galaxy_tags:
        - security
        - fail2ban
        - hardening
        - firewall
        - linux

dependencies: []
